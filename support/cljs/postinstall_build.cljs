;; during the npm-postinstall task for the clojure-script package,
;; this file is compiled, such that it generates core.js and nodejs.js
;; in support/out/cljs/ relative to the package's root directory
(ns postinstall)
