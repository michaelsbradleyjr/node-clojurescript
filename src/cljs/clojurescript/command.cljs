(ns ^{:doc "Command-line interface provider for clojure-script."}
  clojurescript.command
  (:require [cljs.nodejs :as nodejs]
            [clojurescript.helpers :as helpers]))

(defn ^:export run
  []
  )
